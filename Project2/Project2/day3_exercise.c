/*
#include <stdio.h>

int main(void) {
	int score[3];
	printf("0부터 255까지 숫자를 입력하세요 > ");
	scanf_s("%d", &score);
	if (score >= 0 & score <= 255) {
		printf("사용자가 입력한 숫자는 ");
		printf("%d", score);
		return 0;
	}
	else {
		printf("잘 못 입력했습니다. 정확히 입력하세요 \n")
		return -1;
	}
}

int check(int a) {
	while()

}
*/