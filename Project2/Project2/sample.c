/*
#include <stdio.h>

int main(void) {

	int a = 0;
	int b = 0;
	int c = 0;
	scanf("%d %c %d", &a, &b, &c)

	if (b == '+') {
		res = a + c;
	}
	else if (b == '-') {
		res = a - c;
	}
	else if (b == '*') {
		res = a * c;
	}
	else {
		printf("지원되지 않는 형식입니다.");
		return 0;
	}
	printf("%d %c %d = %d", a, b, c, res);
}
*/